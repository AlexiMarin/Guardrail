# ML pipeline: trigger the SageMaker Pipeline (train -> evaluate -> register -> conditional deploy).
# TODO: configure OIDC role and pipeline start step.
name: ml

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths: ["data/**", "training/**", "evaluation/**", "pipelines/**"]

jobs:
  run-pipeline:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TODO: checkout, configure AWS creds (OIDC), start SageMaker Pipeline"
