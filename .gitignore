# Planning / internal docs — kept local, NOT in the portfolio repo
docs/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.ipynb_checkpoints/

# Data — datasets live in S3, never in git
data/raw/
data/curated/
*.csv
*.tsv
*.parquet
*.jsonl
*.arrow

# Model artifacts
*.pt
*.bin
*.safetensors
*.onnx
artifacts/
# quantized model pulled from S3 for the Lambda image build
inference/model/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
# real values (the *.tfvars.example placeholders stay committed)
*.tfvars
# state bucket/account + local creds -- keep out of the public repo
infra/backend.hcl
infra/deploy.env
# terraform build artifacts (kill_switch.zip etc.) -- regenerated, live under any module's .build/
.build/
crash.log

# Secrets / AWS
.env
*.pem
.aws/

# OS / editor
.DS_Store
.vscode/
